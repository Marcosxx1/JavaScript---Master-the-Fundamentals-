 const numeros = [1, 2, 3];

 /* juntando arrays */
 const combinados = numeros.join(',');
 console.log(combinados);

/* utilizando o método .split para separar */
 const mensagem = 'Essa é minha mensagem';
 const partes = mensagem.split(' ');

 const juntos = partes.join('-');
 console.log(juntos);